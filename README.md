# common-hash-benchmark

This project contains the setup for JMH benchmarks to test the performance of
the algorithms in common-hash.

## Usage
`mvn clean package -U && java -jar target/benchmarks.jar`
